<Window x:Class="tutorat.View.StudentRequest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:tutorat.View"
        mc:Ignorable="d"
        Title="StudentRequest" Height="170" Width="219">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel></StackPanel>
        <TextBlock Grid.Row="0"
                   VerticalAlignment="Center" Margin="0,0,8,8"
                   Text="Sujet :"/>
        <TextBox   Grid.Row="0" Grid.Column="1"
                   Margin="0,0,0,8"
                   Text="{Binding SubjectInput, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="1"
                   VerticalAlignment="Center" Margin="0,0,8,8"
                   Text="Catégorie :"/>
        <ComboBox  Grid.Row="1" Grid.Column="1"
                   Name="CboCategorie"
                   Margin="0,0,0,8"
                   ItemsSource="{Binding Categories}"
                   SelectedItem="{Binding SelectedCategory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="2"
                   VerticalAlignment="Center" Margin="0,0,8,8"
                   Text="Note :"/>
        <TextBox   Grid.Row="2" Grid.Column="1"
                   Margin="0,0,0,8"
                   Text="{Binding NoteInput, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="3"
                   VerticalAlignment="Center" Margin="0,0,8,8"
                   Text="DA :"/>
        <TextBox Grid.Row="3" Grid.Column="1"
                   Margin="0,0,0,8"
                   Text="{Binding DaInput, UpdateSourceTrigger=PropertyChanged}"/>

        <Button Grid.Row="4" Grid.Column="1"
                   Content="Envoyer"
                   Width="100"
                   Margin="0,8,0,0"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   Command="{Binding CreateRequestCommand}"/>
        
    </Grid>
</Window>
