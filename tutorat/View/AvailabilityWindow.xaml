<Window x:Class="tutorat.View.AvailabilityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mes disponibilités" Height="250" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Jour de la semaine" />
        <ComboBox Grid.Row="1" 
                  ItemsSource="{Binding Days}" 
                  SelectedItem="{Binding SelectedDay}" 
                  Margin="0,5" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Début:" Margin="0,0,5,0"/>
            <TextBox Width="50" Text="{Binding StartHour}" />
            <TextBlock Text="h" Margin="5,0,5,0"/>
            <TextBox Width="50" Text="{Binding EndHour}" />
            <TextBlock Text="h" Margin="5,0,0,0"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Ajouter disponibilité"
                Command="{Binding SaveCommand}" 
                Width="200" Height="30"
                HorizontalAlignment="Center" Margin="0,15,0,0"/>
    </Grid>
</Window>
